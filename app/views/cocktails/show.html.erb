<div class="banner2" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('https://images.pexels.com/photos/109275/pexels-photo-109275.jpeg?w=940&h=650&auto=compress&cs=tinysrgb');">
  <div class="banner-content">
   <%= link_to 'New cocktail', new_cocktail_path, class: 'btn btn-primary btn-lg' %>
  </div>
</div>

<div class="box"> <div class="content"> <h2><%= @cocktail.name %></h2>
<%= cl_image_tag @cocktail.photo, height: 300, width: 400, crop: :fill %>
<% @cocktail.doses.each do |dose| %>
  <div>
    <p><%= dose.description %> of <%= dose.ingredient.name %></p>
    <%= link_to 'Destroy', dose_path(dose), method: :delete, data: { confirm: 'Are you sure?' } %>
  </div>
<% end %>

<h3>Create new dose</h3>

<%= simple_form_for([@cocktail, @dose]) do |f| %>

  <%= f.input :description %>
  <%= f.association :ingredient, prompt: "Choisissez un ingrédient" %>

  <%= f.button :submit %>
<% end %>
</div>
</div>
